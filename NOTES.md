# Examples
I implemented examples which apply the concepts shown in the fuzzy tutorial.
The examples reside in their own file with suffix `Example` as Jupyter Notebooks.
The examples contain of python code using the fuzzy library and a small description.